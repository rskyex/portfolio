// Font CSS variables - loaded via Google Fonts link in layout
// Fallback font stacks used when fonts are unavailable

export const fontVariables = [
  '--font-kaisei',
  '--font-noto-serif-jp',
  '--font-noto-sans-jp',
] as const;

export const fontClassNames = 'font-sans';
